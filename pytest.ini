[pytest]
# Put the repo root on sys.path so `import app` works regardless of how pytest is
# invoked (`pytest` console script vs `python -m pytest`). Without this, bare
# `pytest` fails to import the app in conftest.py (exit code 4).
pythonpath = .
testpaths = tests
